# Convenience pin-file. The authoritative dependency list is pyproject.toml
# ([project.dependencies] + the `cdp` / `dev` extras); prefer `pip install .`
# or `pip install -e '.[cdp,dev]'` over this file.

# Core (builder_code decode path + all HTTP/RPC calls)
requests>=2.28
cbor2>=5.4
# EIP-55 for the predicted split address before it goes into the 402 as payTo.
eth-utils>=2.0

# Optional: CDP SQL API path (monitor.py) - mints the Bearer JWT
# cdp-sdk>=1.0

# Dev
pytest>=7.0
