# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
# setuptools build artifacts (`pip install ./python`)
build/
dist/

*.db

# Foundry (fork-test/) - deps + build artifacts. Run `forge install` to set up.
fork-test/lib/
fork-test/out/
fork-test/cache/
fork-test/foundry.lock

# Env / secrets (never commit CDP keys or JWTs)
.env
.env.*
