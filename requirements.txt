# Core (builder_code.py decode path + all HTTP/RPC calls)
requests>=2.28
cbor2>=5.4

# Server example (declare + capture + settle observer)
flask>=2.2

# Optional: CDP SQL API path (backfill_sql.py) — mints the Bearer JWT
# cdp-sdk>=1.0

# Dev
pytest>=7.0
